<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% @songs.each do |song| %>
    <div class="song_name">
      <%= link_to song.name, song_path(song.id) %><br>
    <% end %>
    </div>
  </body>
</html>
